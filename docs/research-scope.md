# Research implementation authorization, 2026-09-25

This addendum records the owner's new authorization; it does not replace frozen
P0 or invent the unavailable architecture sections. Continue research workflows
despite the explicitly deferred live NFT promotion-invalidation test. Retain
completed transfer-back evidence and all other genuine passes.

The only additional product capability is one persistent discussion thread with
simple replies inside the existing Workbench. Messages have sources, authors,
specialties and timestamps, and may lead to attributed formal findings. Chat
activity earns no credit. No DMs, channels, presence, voice or chat service.

Six screens: `/join`, `/workbench`, `/findings/new`, `/review`, `/findings/[id]`,
`/membership`. Old submit/contribution URLs redirect. Build data/permissions,
discussion/versioned findings, independent scoped review and atomic awards,
deterministic accepted evidence brief, progression/peer requests and the single
fixed-fee assignment, then browser verification and Chat 05 handoff.

Specialty is self-described focus, not proof of expertise, review authority or
rank. Browser requests must pass the existing live NFT ownership gate. Private
findings are limited to their author and the explicitly authorized scoped review
team; all aggregations operate on already-permitted records.

Illustrative people and sample research remain visibly labeled and separate from
real members, real awards and customer activity. Configurable demo defaults:
accepted initial finding 25 XP and 25 seasonal points; Silver consideration at
75 XP, three accepted findings and one documented cross-specialty use. These are
assessment prerequisites, never automatic promotion or token-conversion promises.
One acceptance award per finding across revisions; corrections do not erase history.

The single assignment is an explicitly unfunded Grindly P0 demonstration, not a
customer commitment. Work acceptance and payment are independent states. No
automated payment, escrow, additional tier or contract is introduced.

Retire only the identified temporary QA Silver/steward fixtures with audit events.
No further NFT-transfer tooling or new transfer test is authorized for this task.
Finish when the collaboration-to-recognition journey works, record incomplete
checks honestly, and prepare a concise independent QA and UI annotation handoff.
